<!--
Below code sets the top border of the buttons.
This layout is set as background property of the buttons.
By setting negative margins for the bottom, right and left borders, the border gets pushed, so only the top border is visible.
By only setting a top border there won't be extra thick lines because of bottom and top borders stacking on top of each other.
-->

<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item>
        <shape android:shape="rectangle" >
            <solid android:color="#FFFFFF" />
        </shape>
    </item>

    <item android:bottom="-2dp" android:right="-2dp" android:left="-2dp">
        <shape>
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="1dp"
                android:color="#49463c" />
        </shape>
    </item>

</layer-list>